<UserControl
    x:Class="JiraGraphThing.Views.ItemProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JiraGraphThing.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Source={StaticResource Locator}, Path=[ItemProgressBarViewModel]}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Frame 
               Grid.Column="0"                
               VerticalContentAlignment="Center"
               VerticalAlignment="Stretch">
            <TextBlock  
                FontSize="20"
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                />
        </Frame>
        <Frame Grid.Column="1" HorizontalContentAlignment="Left" x:Name="ProgressBarHolder">
            <Rectangle x:Name="TimeUsedProgressBar" Height="10" Fill="Red"></Rectangle>
        </Frame>
    </Grid>
               
</UserControl>
